{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm ci && npm run build && echo '✅ Frontend built' && ls -la dist/ && cd ../backend && npm ci && echo '✅ Backend deps installed'"
  },
  "deploy": {
    "startCommand": "echo '🚀 Starting server...' && ls -la /app/frontend/dist/ && cd backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
